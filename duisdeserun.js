// Example of using i = 0 for selecting from a distribution
let i = 0; // Initialize the index
const distribution = [/* array of values representing a distribution */];

// Loop through the distribution
for (i = 0; i < distribution.length; i++) {
    // Selecting elements from the distribution based on the index i
    console.log(distribution[i]);
}
